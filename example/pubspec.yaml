name: screen_time_api_ios_example
description: Demonstrates how to use the screen_time_api_ios plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  screen_time_api_ios:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  very_good_analysis: ^9.0.0

flutter:
  uses-material-design: true
